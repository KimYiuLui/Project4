<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Prototype1.Views.QuizResultaat" NavigationPage.HasNavigationBar="False" BackgroundImage="Background.png">
    <StackLayout Padding="10,20,10,20">
        <Label Text="Doggo List" TextColor="Black" FontAttributes="Bold" FontSize="36" HorizontalOptions="Center"/>
        <ListView x:Name="QuizListView" BackgroundColor="White" ItemTapped="QuizTapped" ItemSelected="QuizSelected">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <TextCell Text="{Binding Name}" />
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </StackLayout>
</ContentPage>